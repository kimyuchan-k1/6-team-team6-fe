interface GroupListLayoutProps {
	children: React.ReactNode;
}

function GroupListLayout(props: GroupListLayoutProps) {
	const { children } = props;
	return <>{children}</>;
}

export default GroupListLayout;
